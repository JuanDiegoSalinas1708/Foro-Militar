@{
    var username = Session["Username"] as string ?? "Invitado";
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewBag.Title - Corvax</title>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <!-- Layout Base -->
    <link rel="stylesheet" href="~/Content/Layout.css" />

    <!-- Component Styles -->
    <link href="~/Content/TopCommunities.css" rel="stylesheet" />
    <link href="~/Content/DashboardPlaceholders.css" rel="stylesheet" />

    <!-- Icons -->
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
</head>

<body>

    <div id="sidebar-backdrop"></div>

    <!-- ============================= -->
    <!-- SIDEBAR -->
    <!-- ============================= -->
    <div id="sidebar">
        <div class="sidebar-header">
            <div class="sidebar-avatar">
                <i class="fa-solid fa-crow"></i>
            </div>
            <div class="sidebar-username">@username</div>
            <div class="sidebar-role">Miembro</div>
        </div>

        <div class="sidebar-nav">
            <a href="@Url.Action("Index","Dashboard")">Inicio</a>
            <a href="#">Comunidades</a>
            <a href="#">Mis Publicaciones</a>
            <a href="#">Configuración</a>
            <a href="@Url.Action("Logout","Auth")">Cerrar Sesión</a>
        </div>

        <div class="sidebar-footer">
            Corvax © @DateTime.Now.Year
        </div>
    </div>

    <!-- ============================= -->
    <!-- TOPBAR -->
    <!-- ============================= -->
    <div id="topbar">
        <div class="hamburger" onclick="toggleSidebar()">
            <i class="fa-solid fa-bars"></i>
        </div>

        <div class="topbar-right">

            <input type="text"
                   placeholder="Buscar..."
                   onclick="toggleSearch()"
                   class="topbar-search" />

            <i class="fa-solid fa-bell topbar-icon"></i>
            <i class="fa-solid fa-gear topbar-icon"></i>

            <a href="#" class="username-link">@username</a>
        </div>
    </div>

    <!-- ============================= -->
    <!-- SEARCH OVERLAY -->
    <!-- ============================= -->
    <div id="search-overlay">
        <div class="search-results">
            <i class="fa-solid fa-crow"></i>
            <p>El cuervo está buscando...</p>
        </div>
    </div>

    <!-- ============================= -->
    <!-- MAIN CONTENT -->
    <!-- ============================= -->
    <div id="page-body">
        @RenderBody()
    </div>

    <!-- ============================= -->
    <!-- FOOTER -->
    <!-- ============================= -->
    <div id="main-footer">
        Sistema Corvax · Arquitectura Modular
    </div>

    @RenderSection("scripts", required: false)

    <script>
        function toggleSidebar() {
            document.getElementById('sidebar').classList.toggle('active');
            document.getElementById('sidebar-backdrop').classList.toggle('active');
        }

        document.getElementById('sidebar-backdrop')
            .addEventListener('click', toggleSidebar);

        function toggleSearch() {
            document.getElementById('search-overlay')
                .classList.toggle('active');
        }
    </script>

</body>
</html>